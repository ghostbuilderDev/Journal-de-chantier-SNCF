const assert=require('node:assert/strict'),{execFileSync}=require('node:child_process'),fs=require('node:fs'),path=require('node:path');
const {setup155}=require('./cr-v155-backend.cjs');
(async()=>{const {PGlite}=require(process.env.PGLITE_MODULE||'@electric-sql/pglite'),db=new PGlite();try{await setup155(db);await db.exec('create table if not exists public.journal_sql_migrations(migration_name text primary key,applied_at timestamptz default now());');const sql=execFileSync(process.env.PYTHON||'python3',['scripts/deploy-v156-release.py','--print-sql'],{cwd:path.resolve(__dirname,'../..'),encoding:'utf8'});await db.exec(sql);await db.exec(fs.readFileSync(path.join(__dirname,'cr-v156-postgres.sql'),'utf8'));assert.equal((await db.query('select count(*)::int n from public.journal_sql_migrations')).rows[0].n,1);console.log('V15.6 real deployment transaction and SQL assertions: OK');}finally{await db.close();}})().catch(e=>{console.error(e.message,e.where||'');process.exit(1)});
