name: Installer le Journal V15.9 - completude partagee et partage QR
on:
  push:
    branches: [main]
    paths:
      - 'supabase/migrations/20260910000200_v15_9_completion_partagee.sql'
      - 'supabase/tests/cr-v159-*'
      - 'scripts/deploy-v159-release.py'
      - 'tests/v159-deployment.test.py'
      - '.github/workflows/deploy-v159.yml'
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: supabase-production
  cancel-in-progress: false
jobs:
  install:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: journal-ci-only
          POSTGRES_DB: journal_v159_test
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U postgres -d journal_v159_test"
          --health-interval 5s --health-timeout 5s --health-retries 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Vérifier le chemin de déploiement avant toute connexion
        run: python3 tests/v159-deployment.test.py
      - name: Vérifier les tables et les droits sur PostgreSQL 17 jetable
        shell: bash
        run: |
          set -euo pipefail
          test_container="${{ job.services.postgres.id }}"
          for file in supabase/tests/backend-fixture.sql supabase/tests/backend-production-shape.sql supabase/migrations/20260906000300_v14_2_contraintes_reelles.sql supabase/tests/feedback-backend-fixture.sql supabase/migrations/20260907000500_v14_5_retours_application.sql supabase/migrations/20260908000100_v15_cr_encadrement.sql supabase/migrations/20260908000200_v15_1_cr_perimetres.sql supabase/migrations/20260908000300_v15_2_cr_simple.sql supabase/migrations/20260909000100_v15_3_cr_tables.sql supabase/migrations/20260909000200_v15_4_production_email.sql supabase/migrations/20260909000300_v15_5_requests_pdf.sql supabase/migrations/20260909000400_v15_6_writing_references_delete.sql supabase/migrations/20260909000500_v15_7_reference_catalogue.sql supabase/migrations/20260910000100_v15_8_preparation_briefing.sql; do
            docker exec -i "$test_container" psql -U postgres -d journal_v159_test -v ON_ERROR_STOP=1 < "$file"
          done
          docker exec -i "$test_container" psql -U postgres -d journal_v159_test -v ON_ERROR_STOP=1 -c "CREATE TABLE IF NOT EXISTS public.journal_sql_migrations (migration_name text PRIMARY KEY, applied_at timestamptz NOT NULL DEFAULT now());"
          python3 scripts/deploy-v159-release.py --print-sql | docker exec -i "$test_container" psql -U postgres -d journal_v159_test -v ON_ERROR_STOP=1
          docker exec -i "$test_container" psql -U postgres -d journal_v159_test -v ON_ERROR_STOP=1 < supabase/tests/cr-v159-postgres.sql
          docker exec -i "$test_container" psql -U postgres -d journal_v159_test -v ON_ERROR_STOP=1 -c "DO \$\$ BEGIN IF (SELECT count(*) FROM public.journal_sql_migrations WHERE migration_name='20260910000200_v15_9_completion_partagee.sql') <> 1 THEN RAISE EXCEPTION 'Migration non enregistrée'; END IF; END \$\$;"
      - name: Vérifier les contributions simultanées et les droits
        run: |
          npm install --prefix /tmp/journal-v159-tests @electric-sql/pglite@0.5.8 --ignore-scripts --no-audit --no-fund
          PGLITE_MODULE=/tmp/journal-v159-tests/node_modules/@electric-sql/pglite node supabase/tests/cr-v159-backend.cjs
          PGLITE_MODULE=/tmp/journal-v159-tests/node_modules/@electric-sql/pglite node supabase/tests/cr-v159-deployment.cjs
          python3 tests/v159-installation.test.py
      - name: Installer le client PostgreSQL
        run: |
          if ! command -v psql >/dev/null; then
            sudo apt-get update -qq
            sudo apt-get install -y postgresql-client
          fi
      - name: Appliquer V15.9 et contrôler les droits
        env:
          SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: python3 scripts/deploy-v159-release.py
